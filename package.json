{
  "private": true,
  "scripts": {
    "build": "spago build",
    "pulp-build": "pulp build",
    "test": "spago -x ./test.dhall test",
    "editor": "spago -x ./editor.dhall bundle-app --to audiograph-editor/dist/app.js",
    "simple-examples": "spago -x ./simple-examples.dhall bundle-app --to simple-examples/dist/app.js"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "spago": "^0.20.3",
    "purescript": "^0.14.3",
    "purescript-psa": "^0.6.0",
    "webpack": "^3.6.0"
  }
}
